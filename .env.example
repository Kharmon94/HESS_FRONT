# Copy to .env.local for local development (optional).
# REQUIRED for production builds: set VITE_API_URL in the hosting provider (Railway, Vercel, etc.)
# at build time. Without it, /api requests go to the static site and never reach Rails.

# Rails API base URL (no trailing slash)
# VITE_API_URL=http://localhost:3000
